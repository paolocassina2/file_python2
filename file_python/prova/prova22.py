def cajkdfj():
	import random
	a=[]
	for r_n in random.sample(range(1, 7), 6):
		# ~ print(random_number)
		
	   
		a=a+[r_n]

	print(a)
	while True:
		b=[]

		for r_n ,j in zip(random.sample(range(1, 7), 6),a):
			if r_n!=j:
				b=b+[r_n]
				
		# ~ print(b)
				
		if len(b)==6:
			break
				
	print(b)
	from itertools import product
import random
import numpy as np
m=[]
c1,c2,c3,c4=[],[],[],[]

# ~ import numpy
# ~ for x1 in range(len(m)):
	# ~ if x1==1:
		# ~ c1.append(


# ~ print(m1)



# ~ print(m1)
print("dFdfdfdfdfdffd")

while True:
	c=0

	for  x in range(6):
			a=[]
			for r_n in random.sample(range(1, 7), 6):
			# ~ print(random_number)
		
	   
				a=a+[r_n]
	# ~ if range>1:

	# ~ if x in range(1):		
			
			m.append(a)
		
	m1 = np.array(m)	
		

	# ~ print(m1)
	m=[]
	# ~ print(c)
	# ~ print()
	# ~ print()
	# ~ print()
	# ~ if len(set(list(sudoku.var[:,0])))==sudoku.var[:,0].size and len(set(list(sudoku.var[:,1])))==sudoku.var[:,1].size
	# ~ print(c)
	for x in range(6):
		# ~ print(m1[:,x])e
		dim_colonna=m1[:,x].size
		colonna=list(m1[:,x])
		# ~ print(type(colonna))
		if len(set(list(m1[:,x])))==dim_colonna:
			c=c+1
			if c>=5:
				print(m1)
	
	if c>=5:
		break
		# ~ c=0
				# ~ break
	# ~ print(c)
	# ~ c=0		
				# ~ break
			# ~ else:c=0
# ~ print(c)		
# ~ if c>3:
	# ~ print(m1)
		# ~ break
	# ~ c=0
	# ~ print(c)
	# ~ print(m1)	
	# ~ c=0
	# ~ print(colonna1)
	# ~ print(set(colonna))
	# ~ if ==len(list(set(list(m1[:,x])))):
		# ~ c=c+1
		# ~ if c==4:
			# ~ print(m1)
			# ~ break
			
		
# ~ def c1 (): lambda x: x not in zip(a, b, c)
	# ~ return x
# ~ print(c1e
