#SISTEMARE TENTATIVI-->si devono ridurre solo se lettera inserita non è giusta

#COSA AGGIUNGERE: SI DEVE CONTINUARE A GIOCARE SIA CHE SI VINCA CHE SI PERDA. quando finisce una partita il programma deve generare una nuova parola.
# ~ def x1():
		# ~ x1.var=""
# ~ x1()
def x1():
		x1.var=""
x1()


# ~ def canvas():
		# ~ canvas.var=""
# ~ canvas()
# ~ if vero!=33 
def indovinalettera():
	indovinalettera.var=""
	
indovinalettera()


def label2():
	label2.var=""
label2()
def label3():
	label3.var=""
label3()

def abcdefg():
	abcdefg.var=0
abcdefg()

def canvas1():
	canvas1.var=""
	
canvas1()


def canvas():
		canvas.var=""
canvas()
# ~ if vero!=33 
# ~ def indovinalettera():
	# ~ indovinalettera.var=""
	
# ~ indovinalettera()

# ~ def label1():
	# ~ label1.var=""
# ~ label1()

# ~ def label2():
	# ~ label2.var=""
# ~ label2()
# ~ def label3():
	# ~ label3.var=""
# ~ label3()
def label6():
	label6.var=""
label6()
# ~ def abcdefg():
	# ~ abcdefg.var=0
# ~ abcdefg()
# ~ def contadisegno1():
		
		# ~ contadisegno1.var=0
# ~ contadisegno1()
def canvas1():
	canvas1.var=""
	
canvas1()

# ~ def listastringa1():
	# ~ listastringa1.var=""
# ~ listastringa1()
# ~ giocoimp_()

# ~ def incr():
	# ~ contadisegno1.var=contadisegno1.var+1

		
		# ~ label1.var = tk.Label(root, text="")
		# ~ label1.var.config(font=('helvetica', 14))
		# ~ canvas1.var.create_window(400, 70, window=label1.var
		
def a():
	a.var=""
a()

# ~ print(stringa3.var)	
	
# ~ lista_di_a=list(a.var)
def stringa3():
	stringa3.var=""
stringa3()

def stringa():
	stringa.var=""
stringa()
indice=-1

def parola_random():
	parola_random.var=""
parola_random()		

# ~ from random_word import RandomWords		# ~ elif contadisegno1.var==12:
# ~ parola_random.var=RandomWords()

# ~ def aa():
	# ~ aa.var=""
# ~ aa()
# ~ aa=parola_random.var.get_random_word()
# ~ a.var=aa.lower()
parole_gia_uscite=[]
# ~ while True:

			# ~ if ciao!=x:
				# ~ parola_nascosta+=y
		# ~ else:
			# ~ parola_nascosta+=ciao
		# ~ if x==ciao:
			# ~ parola_nascosta=parola_nascosta.replace("*",x)
		# ~ print(x)
		# ~ print(y)
		# ~ print(x)
	# ~ print(parola_nascosta)
	# ~ testo= parola_nascosta.replace("*", ciao)
	# ~ print(testo)
# ~ def ciao():
	
	
	
def contadisegno1():
		
			contadisegno1.var=0
contadisegno1()	
	

def creaparola():
	path="C:\\Users\\Attilio\\Documents\\R\\win-library\\4.1\\vroom\\words\\animals.txt"
	with open(path) as f:
		lines = f.readlines()
	parole=[xj for xj in lines]
	# ~ print(parole)
	import random
	def parola2():
		parola2.var=""
	parola2()

	while True:
		parola2.var=random.choice(parole)
		if parola2.var not in parole_gia_uscite:
			break

	print(parola2.var)
	a.var=parola2.var.strip()
	parole_gia_uscite.append(a.var)			# ~ if ciao in a.var:

	def parola_nascosta():
		parola_nascosta.var=""
	parola_nascosta()	
	# ~ parola_nascosta.var=""
	c=-1
	for xz in a.var:
			c=c+1
			if c==0 or c==(len(a.var)-1):
				parola_nascosta.var=parola_nascosta.var+xz
			else:
				parola_nascosta.var=parola_nascosta.var+"*"
				
creaparola()	
				
def disegna_disegno():
	# ~ while True:
		# ~ contadisegno1.var=contadisegno1.var+1
			if contadisegno1.var<=11:	
				if contadisegno1.var==1:
					canvas1.var.create_line(200, 400, 400,400)
					# ~ time.sleep(1000)
					# ~ time.sleep(100)
				elif contadisegno1.var==2:
					canvas1.var.create_line(300, 400, 300, 200)
				# ~ time.sleep(1000)
				# ~ time.sleep(100)
				elif contadisegno1.var==3:
					canvas1.var.create_line(300, 200, 400, 200)
				# ~ time.sleep(100)
				elif contadisegno1.var==4:
					canvas1.var.create_line(400, 200, 400, 250)		
				elif contadisegno1.var==5:
					canvas1.var.create_oval(380, 250, 430,280, width=2)
				elif contadisegno1.var==6:
					canvas1.var.create_oval(390, 270, 410,280, width=2)
				elif contadisegno1.var==7:
					canvas1.var.create_line(400, 280, 400, 310,width=2)
				elif contadisegno1.var==8:
					canvas1.var.create_line(400, 290, 410, 300,width=2)
				elif contadisegno1.var==9:
					canvas1.var.create_line(400, 290, 390, 300,width=2)
				elif contadisegno1.var==10:
					canvas1.var.create_line(400, 310, 410, 320,width=2)
				elif contadisegno1.var==11:
					canvas1.var.create_line(400, 310, 390, 320,width=2)
					abcdefg.var=1
					print("hai perso")			
# ~ print("p",parola_nascosta.var)	
# ~ print(len(parola_nascosta.var),len(a.var))
# ~ print(a.var)		# ~ parola_nascosta=parola_nascosta.replace("*",ciao)
			# ~ for x ,y in zip(a.	
			# ~ canvas1.var.create_text(100, 70, text = 'Welcome', font = ('Helvetica', 40, 'bold'), justify = 'center', fill='blue')
def entrybox():
			# ~ c=0
				
	
		# ~ canvas1.var.create_text(80, 70, text ="" , font = ('Helvetica', 20, 'bold'), justify = 'center', fill='blue')
		# ~ canvas1.var.destroy()
		# ~ canvas1.var.create_text(80, 70, text =str(stringa3.var) , font = ('Helvetica', 20, 'bold'), justify = 'center', fill='blue')
		# ~ print("stringa3.var",stringa3
		# ~ var,parola_nascosta):
				# ~ b=b+1
			# ~ while True:
		import tkinter as tk
			
		def label1():
			label1.var=""
		label1()

		
					

	#################################################
		root= tk.Tk()
		
		
		
		canvas1.var = tk.Canvas(root, width = 1500, height =800,  relief = 'raised')
		canvas1.var.pack()
		
	
		def game():
			# ~ print()
			
			# ~ print("numero lettere ",len(a.var))
			# ~ def ciao():
			# ~ print(parole)
			
			# ~ print("aa",aa)
			# ~ print(parola_nascosta)
			# ~ while True:
				# ~ indovinato="no"

				
				# ~ print(parola_nascosta)
				# ~ print(len(parola_nascosta),len(aa))
				# ~ print(a.var)
				# ~ ciao=x.var
				# ~ for x in a:
					# ~ if ciao==x:
				# ~ b=-1
# ~ def ciao():
			
				parola_nascosta1=""
				for xj,y in zip(a.var,parola_nascosta.var):
					if x1.var!=xj:
						parola_nascosta1+=y
						
						# ~ print(contadisegno1.var)  #disegno anche il disegno dell'impiccato
								#se non indovino uguale aggiungo *
					else:
						parola_nascosta1+=x1.var
							#se indovino la lettera aggiungo la lettera
				# ~ print(parola_nascosta1)
				parola_nascosta.var=parola_nascosta1
				# ~ print("ciao",parola_nascosta.var)
				# ~ disegna_disegno()
					# ~ break
				# ~ print()
	
				
# ~ entrybox()				
# ~ def ciao():
		entry1 = tk.Entry (root) 								#SPAZIOinput
		canvas1.var.create_window(250,40, window=entry1)
		label1.var = tk.Label(root, text="")
		label1.var.config(font=('helvetica', 14))
		canvas1.var.create_window(40, 80, window=label1.var)	
		label6.var = tk.Label(root, text=parola_nascosta.var)
		label6.var.config(font=('helvetica', 14))
		canvas1.var.create_window(40, 200, window=label6.var)	
		
		def scrivilabel():
			# ~ if label1.var is not str:
			
				x1.var = entry1.get()
				print("ciao",a.var)
				if x1.var not in a.var:
					# ~ pass
				# ~ else:
					contadisegno1.var=contadisegno1.var+1
					print(contadisegno1.var,"ciao")	
					disegna_disegno()
				game()
				label1.var.destroy()
				label1.var = tk.Label(root, text="lettera inserita	"+ x1.var)
				label1.var.config(font=('helvetica', 14))
				canvas1.var.create_window(500, 80, window=label1.var)	
				
				label6.var.destroy()
				# ~ x1.var = entry1.get()
				game()
				label6.var = tk.Label(root, text="parola	"+parola_nascosta.var)
				label6.var.config(font=('helvetica', 14))
				canvas1.var.create_window(500, 200, window=label6.var)	
				if "*" not in parola_nascosta.var:
					label3.var = tk.Label(root, text="hai vinto")
					label3.var.config(font=('helvetica', 14))
					canvas1.var.create_window(500, 400, window=label3.var)	
					print("hai vinto")
				elif contadisegno1.var>=11:
					label3.var = tk.Label(root, text="hai perso")
					label3.var.config(font=('helvetica', 14))
					canvas1.var.create_window(500, 400, window=label3.var)		
				# ~ disegna_disegno()
				# ~ else:
				# ~ if contadisegno1.var>11:
				# ~ label3.var = tk.Label(root, text="hai perso")
				# ~ label3.var.config(font=('helvetica', 14))
# ~ c				anvas1.var.create_window(500, 400, window=label3.var)	
					# ~ print("hai perso")
				# ~ game()
		# ~ a=str(stringa3.var)
		# ~ root.after(1000, incr)
		# ~ root.after(1000, disegna_disegno)
		# ~ import time
		# ~ c=c+1
		# ~ contadisegno1.var+=1
		# ~ time.sleep(1)	
		# ~ disegna_disegno()
	# ~ e	label6.var=tk.Label(root, text=contadisegno1.var)
	
		button1 = tk.Button(text='scegli_lettera', command=scrivilabel, bg='brown', fg='white', font=('helvetica', 9, 'bold'))
		canvas1.var.create_window(50,50, window=button1)	
	
	
		# ~ root.after(1000, game)
		root.mainloop()
# clear the canvas 
entrybox()
# ~ ciao()
	# ~ giocoimp_()
		
	# ~ time.sleep(1000)    
	# ~ canvas1.create_line(200, 100, 300, 100)
	# ~ time.sleep(1000)       
	# ~ canvas1.create_line(300, 100, 300, 150)
	# ~ canvas1.create_oval(280, 150, 330,180, width=2)
	# ~ canvas1.create_oval(290, 170, 310,180, width=2)
	# ~ canvas1.create_line(300, 180, 300, 210,width=2)
	# ~ canvas1.create_line(300, 190, 310, 200,width=2)
	# ~ canvas1.create_line(300, 190, 290, 200,width=2)
	# ~ canvas1.create_line(300, 210, 310, 220,width=2)
	# ~ canvas1.create_line(300, 210, 290, 220,width=2)




	# ~ root.mainloop()




		# ~ uno()
		# ~ time.sleep(2)
		# ~ due()
		# ~ time.sleep(2)
		# ~ tre()
		# ~ time.sleep(2)



					
				
				# ~ break
					
			# ~ if contadisegno1.var==11:
				
			# ~ conta=9
		# ~ l=[1,4,5,1,1,3,88,5]		
		# ~ print("restituiscituttiindici",[index for index, value in enumerate(l) if value == 5])
		# ~ print(listadiindici)
	# ~ print(stringa)
	# ~ print(stringa)		
		# ~ print(listadiindici)
			# ~ stringa.replace("a",indovinalettera)	
			# ~ rivelaparola=str(a[0])+str(a[a.index(i)])+str(a[len(a)-1])
	# ~ rivelaparola
			# ~ print(rivelaparola)
# ~ ciao()
